<div class="index-container">
  <h3>Recently added songs:</h3>
  {{!-- {{#each @model as |artist|}}
    <h4>{{artist.name}}</h4>
    <img src={{artist.image}} alt={{artist.name}}/>
      {{#each artist.albums as |album|}}
        <h4>{{album.title}} {{album.releaseDate}}</h4>
      {{/each}}
  {{/each}} --}}
  {{outlet}}
</div>